"""Package runner for the `socket_email` project.

Provides a minimal entrypoint that explains how to invoke submodules.
"""

def main():
    """Print a short hint about available submodules and entrypoints."""
    print(
        "socket_email package entry. Use submodules, e.g., server_control.main or telepc.client."
    )


if __name__ == "__main__":
    main()
